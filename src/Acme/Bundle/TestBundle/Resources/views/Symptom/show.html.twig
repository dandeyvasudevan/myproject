{% extends 'AcmeTestBundle::layout.html.twig' %}

{% block content %}
    <h1>Symptom</h1>

    <table class="table-bordered" style="width:100%;">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ entity.name }}</td>
            </tr>
        </tbody>
    </table>

    <br />
        <a href="{{ path('symptom') }}" class="btn">
            Back to the list
        </a>
    
        <a href="{{ path('symptom_edit', { 'id': entity.id }) }}" class="btn">
            Edit
        </a>
    {{ form(delete_form) }}
</ul>
{% endblock %}
